package Dao;
import Model.Bill;
/**
 * The DAO (Data Access Object) class for the Bill model.
 * Extends the AbstractDAO class to inherit common database operations.
 */
public class BillDAO extends AbstractDAO<Bill>{
}