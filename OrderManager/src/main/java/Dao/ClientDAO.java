package Dao;
import Model.Client;
/**
 * The DAO (Data Access Object) class for the Client model.
 * Extends the AbstractDAO class to inherit common database operations.
 */
public class ClientDAO extends AbstractDAO<Client>{
}